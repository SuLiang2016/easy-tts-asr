# Hello TTS - 语音工坊

基于火山引擎语音大模型的个人语音工坊：文字转语音、语音转文字、换声、AI 润色。纯 Web 端，无用户体系，无需登录。

## Language

### 核心能力

**合成（TTS）**:
把文字用选定音色变成语音。文本上限 1000 字符。
_Avoid_: 生成朗读、配音（指任务时易与其他概念混淆）

**识别（ASR）**:
把音频变成文字。音频上限 5 分钟 / 20MB。
_Avoid_: 转写、听写

**换声**:
识别（ASR）→ 合成（TTS）的串行流水线：先识别原声文字，再用目标音色重新合成。源音频上限 60 秒。
_Avoid_: 变声（易与实时变声器混淆）、克隆（本产品不做音色复刻）

**润色**:
用用户自配的 OpenAI 兼容大模型改写文字，使其更适合朗读。与火山无关，配置存浏览器本地。

### 音色

**音色（Voice）**:
一个可选的合成声音，有唯一 id（如 `zh_female_vv_uranus_bigtts`）。服务端按白名单校验，白名单与前端展示共用一份音色表。

**精品音色 / 特色音色**:
音色的两个展示分组；前者是常规男女声，后者是卡通、方言等娱乐音色。仅影响 UI 分组，不影响接口。

### 调节参数

**倍率（语速 / 音量 / 音调）**:
UI 滑杆使用的值，1.0 表示不变，范围 0.5–2.0。发给火山前映射为接口偏移量：语速/音量线性映射到 `speech_rate`/`loudness_rate`（[-50, 100]），音调按半音映射到 `pitch`（[-12, 12]）。倍率等于 1.0 时对应字段不下发。

### 界面状态

**历史（生成历史 / 识别历史 / 换声历史）**:
当前页面会话内累积的结果列表，仅存组件内存，刷新即失。历史项若含音频，其 blob URL 的创建与释放由统一的 history hook 管理。

**工作区**:
页面主输入区（TTS 的文本框、ASR 的文字区）。从历史加载内容到工作区称为**加载**。

**未保存更改（dirty）**:
工作区内容与最近一次加载/生成的锚点快照不一致的状态。此时切换或清空历史需要用户确认。

**就地播放**:
在历史条目内部直接播放该条音频，与主播放器互斥（一开播就暂停另一个）。
